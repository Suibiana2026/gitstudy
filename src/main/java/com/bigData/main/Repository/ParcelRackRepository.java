package com.bigData.main.Repository;

import com.bigData.main.pojo.ParcelRack;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ParcelRackRepository extends JpaRepository<ParcelRack, Long> {
}
